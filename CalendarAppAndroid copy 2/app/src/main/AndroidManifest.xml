<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="home.eduard.calendarappandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewAct"
            android:label="New Activity"></activity>
        <activity android:name=".ViewEditAppointment"
            android:label="View/Edit Appointment"></activity>
        <activity android:name=".Delete"
            android:label="Delete appointment"></activity>
        <activity android:name=".Search"
            android:label="Search appointment"></activity>
        <activity android:name=".Picker"
            android:label="Select appointment"></activity>
        <activity android:name=".Move"
            android:label="Move appointment"></activity>
    </application>

</manifest>